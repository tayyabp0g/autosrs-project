# Backend environment example
PORT=5000
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=YOUR_MYSQL_PASSWORD_HERE
DB_NAME=chatbot_db
JWT_SECRET=change_this_to_a_strong_secret
JWT_EXPIRE=7d
NODE_ENV=development
# CORS origin(s) allowed (use * for development)
CORS_ORIGIN=http://localhost:3000
